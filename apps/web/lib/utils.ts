/**
 * Common utilities for the application
 * Re-exports from specialized utility modules to provide backward compatibility
 */

// Re-export cn from ui/utils to avoid duplicate implementations
export { cn } from './ui/utils';
