/**
 * Re-export từ components/ui/toast để quản lý tập trung mà không phá vỡ cấu trúc hiện có
 * Trong tương lai, nên chuyển tất cả UI hooks vào thư mục này
 */

export { useToast, toast } from '@/components/ui/use-toast'; 